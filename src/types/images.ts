export type ImageData = {
  id: string;
  author: string;
  width: number;
  height: number;
  url: string;
  download_url: string;
};

export type FavoriteImageData = {
  id: string;
  url: string;
  author: string;
};
